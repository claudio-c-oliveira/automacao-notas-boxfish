================================================================================
CORRECAO DO CAMINHO - REMONTAR EM /home/node/.n8n-files/box-fish-config
================================================================================
O caminho que eu te passei antes (/data/box-fish-config) NAO funciona. Eu errei:
minha sonda anterior era invalida e me deu o resultado invertido.

O que descobri agora, testando de verdade (gravando um arquivo em cada pasta e
lendo de volta): o n8n usa uma LISTA DE PERMISSAO por prefixo, nao uma lista de
bloqueio. So passam:

    /home/node/.n8n-files/   (e subpastas)
    /files/                  (e subpastas)

Bloqueados: /data, /tmp, /home/node/, /home/node/.n8n/

Ou seja: sua sugestao original de .n8n-files estava certa desde o inicio.

BOA NOTICIA: os arquivos ja estao em /opt/box-fish-config no host e continuam
la. Nao precisa copiar nada de novo - so trocar o destino do mount.

Cole UM comando por vez.

--------------------------------------------------------------------------------
PASSO 1 - conferir que os arquivos continuam no host (nao mexemos neles)
--------------------------------------------------------------------------------
ls -la /opt/box-fish-config

  Esperado: os 5 JSONs (config_execucao, apelidos, cobrancas, log_diario,
  pendencias_identificacao).

  >>> SO CONTINUE SE ELES ESTIVEREM AI. <<<

--------------------------------------------------------------------------------
PASSO 2 - recriar o container com o mount no lugar certo
--------------------------------------------------------------------------------
sudo docker stop n8n

sudo docker rm n8n

sudo docker run -d --restart unless-stopped --name n8n -p 5678:5678 -e N8N_HOST=n8n.carvalhodeoliveira.com -e N8N_PROTOCOL=https -e WEBHOOK_URL=https://n8n.carvalhodeoliveira.com/ -e N8N_EDITOR_BASE_URL=https://n8n.carvalhodeoliveira.com/ -e NODE_FUNCTION_ALLOW_EXTERNAL=exceljs -e NODE_PATH=/usr/local/lib/node_modules_extra/node_modules -v n8n_data:/home/node/.n8n -v /opt/box-fish-config:/home/node/.n8n-files/box-fish-config n8n-custom:latest

  (unica diferenca pro comando anterior: o destino do ultimo -v mudou de
   /data/box-fish-config para /home/node/.n8n-files/box-fish-config)

--------------------------------------------------------------------------------
PASSO 3 - conferir
--------------------------------------------------------------------------------
sudo docker exec n8n ls -la /home/node/.n8n-files/box-fish-config

sudo docker logs --tail 20 n8n

--------------------------------------------------------------------------------
DEPOIS
--------------------------------------------------------------------------------
Me avise que eu sondo de novo. Desta vez a sonda testa LEITURA E ESCRITA de
arquivos que existem de verdade - foi exatamente o que faltou na anterior.

Pra trocar ambiente/modo/limite, continua editando no host, sem docker exec:

sudo nano /opt/box-fish-config/config_execucao.json

--------------------------------------------------------------------------------
LIMPEZA (opcional, so depois de tudo validado)
--------------------------------------------------------------------------------
sudo docker exec n8n rm -rf /home/node/.n8n/box-fish-config

  NAO rode isso agora. E o seu backup se algo der errado.
  E nunca apague o volume n8n_data.
